venv/
.venv/
__pycache__/
*.py[cod]
.DS_Store

# Local environment variables and secrets
.env

# CSV data files
*.csv
!data/application_demo.csv
!/data/kspo_license_grades.csv
!/data/kspo_license_eligibility_paths.csv

# 자격제도안내는 DB가 아닌 CSV로만 보관한다 (crawl_license_info로 생성).
# exam_info 화면이 이 두 파일을 못 읽으면 "정보를 아직 불러오지 못했습니다"로 표시된다.
!/data/kspo_license_grades.csv
!/data/kspo_license_eligibility_paths.csv

# 대시보드 시연용 더미 신청 현황. DB 용량을 늘리지 않기 위해 DB가 아닌 CSV로만 관리하며,
# 실제 ApplicationStatus 데이터가 없을 때만 dashboard() 뷰가 이 파일을 읽어 화면에 보여준다.
!/data/dashboard_demo_applications.csv

# Local database files
*.db
*.db-*
*.sqlite
*.sqlite-*
*.sqlite3
*.sqlite3-*
*.sqlite3.gz

# 현재 Django 서비스가 실제 사용하는 배포용 DB만 Git에 포함한다.
!/sports_service_202607.sqlite3

# Collected static output
staticfiles/
